spring:
  application:
    name: source
  cloud:
    config:
      fail-fast: true
      retry:
        multiplier: 1.5
        initial-interval: 1000
        max-interval: 5000
        max-attempts: 8
eureka:
  client:
    serviceUrl:
      defaultZone: http://${EUREKA_SERVER}/eureka